{/*
  {feederContainerVisible &&( 
    <div>
  <label className="week-caption">Select a Week:{selectedWeek}</label>
    <Slider
      min={1}
      max={52}
      value={selectedWeek}
      onChange={value => setSelectedWeek(value)}
    />
    </div>
      )}
  */}